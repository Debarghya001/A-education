import 'package:flutter/material.dart';
class R1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    appBar: AppBar(
    title: Text('Assingment Remainder'),
    backgroundColor: Colors.cyan[800],
),
    );
  }
}
